version: '3'
services:
  devinhousepostgres:
    image: postgres
    restart: always
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: devinhouse2
      POSTGRES_PASSWORD: postgres
    ports:
      - 5434:5432